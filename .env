# Point this at the real backend. Must include the /api prefix — every
# backend route is registered under /api/... (see backend/src/app.ts).
VITE_API_BASE_URL=http://localhost:5000/api

# Set to "false" now that the backend above is reachable. While "true",
# every screen runs off src/api/stub instead of hitting the network.
VITE_USE_STUB_API=false
